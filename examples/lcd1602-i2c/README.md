# LCD1602 I2C example

Drives a 16x2 HD44780-compatible LCD through a PCF8574 I2C backpack at address `0x3F`.

Typical Arduino Uno wiring:

- LCD `VCC` -> `5V`
- LCD `GND` -> `GND`
- LCD `SDA` -> `A4`
- LCD `SCL` -> `A5`

Build and flash this example like the other examples in this repository.
