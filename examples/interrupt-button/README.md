# interrupt-button

Toggles the built-in LED on D13 from an external interrupt on D2 (INT0).
Wire a push button between D2 and GND; the example enables the internal pull-up
and triggers on the falling edge.
